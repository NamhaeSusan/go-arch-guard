version: "2"

linters:
  enable:
    - gocritic
    - errorlint
    - misspell
  settings:
    gocyclo:
      min-complexity: 35

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
